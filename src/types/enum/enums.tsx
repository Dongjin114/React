export enum OrmState {
  JPA = "JPA",
  QUERYDSL = "QUERYDSL",
  MYBATIS = "MYBATIS",
}

export enum ButtonType {
  INHERIT = "inherit",
  PRIMARY = "primary",
  SECONDARY = "secondary",
  SUCCESS = "success",
  ERROR = "error",
  INFO = "info",
  WARNING = "warning",
}

export enum PostStatus {
  EDIT = "edit",
  CREATE = "create",
  NONE = "none",
}

export enum ButtonName {
  SEARCH = "조회",
  CREATE = "등록",
  EDIT = "수정",
  DELETE = "삭제",
}
